import 'package:flutter/material.dart';

class MultipleModePage extends StatelessWidget {
  const MultipleModePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Multiple mode'),
      ),
    );
  }
}
