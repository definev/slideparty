import 'package:flutter/material.dart';

class OnlineGridPlayboard extends StatelessWidget {
  const OnlineGridPlayboard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
