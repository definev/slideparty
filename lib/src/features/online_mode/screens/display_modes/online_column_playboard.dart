import 'package:flutter/material.dart';

class OnlineColumnPlayboard extends StatelessWidget {
  const OnlineColumnPlayboard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
