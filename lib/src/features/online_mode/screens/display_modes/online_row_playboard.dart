import 'package:flutter/material.dart';

class OnlineRowPlayboard extends StatelessWidget {
  const OnlineRowPlayboard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
