import 'package:flutter/material.dart';

class OnlineModePage extends StatelessWidget {
  const OnlineModePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Online mode'),
      ),
    );
  }
}
